import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_for_mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
